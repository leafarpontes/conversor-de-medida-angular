import { MilimetroParaCentimetroPipe } from './milimetro-para-centimetro.pipe';

describe('MilimetroParaCentimetroPipe', () => {
  it('create an instance', () => {
    const pipe = new MilimetroParaCentimetroPipe();
    expect(pipe).toBeTruthy();
  });
});
