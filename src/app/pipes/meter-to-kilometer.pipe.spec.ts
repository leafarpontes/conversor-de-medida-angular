import { MeterToKilometerPipe } from './meter-to-kilometer.pipe';

describe('MeterToKilometerPipe', () => {
  it('create an instance', () => {
    const pipe = new MeterToKilometerPipe();
    expect(pipe).toBeTruthy();
  });
});
