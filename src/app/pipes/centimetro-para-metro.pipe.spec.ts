import { CentimetroParaMetroPipe } from './centimetro-para-metro.pipe';

describe('CentimetroParaMetroPipe', () => {
  it('create an instance', () => {
    const pipe = new CentimetroParaMetroPipe();
    expect(pipe).toBeTruthy();
  });
});
