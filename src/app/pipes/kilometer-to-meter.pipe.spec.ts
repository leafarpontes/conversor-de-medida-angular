import { KilometerToMeterPipe } from './kilometer-to-meter.pipe';

describe('KilometerToMeterPipe', () => {
  it('create an instance', () => {
    const pipe = new KilometerToMeterPipe();
    expect(pipe).toBeTruthy();
  });
});
