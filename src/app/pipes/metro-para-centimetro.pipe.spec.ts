import { MetroParaCentimetroPipe } from './metro-para-centimetro.pipe';

describe('MetroParaCentimetroPipe', () => {
  it('create an instance', () => {
    const pipe = new MetroParaCentimetroPipe();
    expect(pipe).toBeTruthy();
  });
});
