import { CentimetroParaMilimetroPipe } from './centimetro-para-milimetro.pipe';

describe('CentimetroParaMilimetroPipe', () => {
  it('create an instance', () => {
    const pipe = new CentimetroParaMilimetroPipe();
    expect(pipe).toBeTruthy();
  });
});
